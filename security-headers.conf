# Baseline security headers, included in every location that declares its own
# add_header (nginx: any add_header in a location DISCARDS inherited ones, so
# server-level alone silently stops applying — include this snippet instead).
add_header X-Content-Type-Options "nosniff" always;
add_header X-Frame-Options "SAMEORIGIN" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
