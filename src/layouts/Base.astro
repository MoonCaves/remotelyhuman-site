---
interface Props {
  title: string
  description?: string
}
const { title, description = 'The official home of Remotely Human. We work quietly across many projects.' } = Astro.props
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href="/favicon.ico" sizes="48x48" />
    <link rel="icon" href="/brand/rh-mark-padded.svg" type="image/svg+xml" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <script is:inline>
      // Purple is the default; ?theme=green sticks as the comparison variant,
      // any other ?theme= value resets to default.
      const themeParam = new URLSearchParams(location.search).get('theme')
      if (themeParam === 'green') localStorage.setItem('rh-theme', 'green')
      else if (themeParam) localStorage.removeItem('rh-theme')
      if (localStorage.getItem('rh-theme') === 'green')
        document.documentElement.dataset.theme = 'green'
    </script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,400;0,500;0,600;0,700;0,800;1,400&family=Plus+Jakarta+Sans:wght@600;700;800&family=Instrument+Serif:ital@0;1&family=IBM+Plex+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --bg: #070b0a;
    --panel: #0d1512;
    --ink: #eef2f0;
    --dim: rgba(255, 255, 255, 0.7);
    --faint: rgba(255, 255, 255, 0.45);
    --accent: #6c2bd9;
    --accent-hover: #7d3ee8;
    --accent-text: #a78bfa;
    --accent-contrast: #ffffff;
    --hairline: rgba(255, 255, 255, 0.1);
    --font-display: 'Inter', system-ui, sans-serif;
    --font-body: 'Inter', system-ui, sans-serif;
    --font-jakarta: 'Plus Jakarta Sans', system-ui, sans-serif;
    --font-serif: 'Instrument Serif', Georgia, serif;
    --font-mono: 'IBM Plex Mono', ui-monospace, monospace;
  }

  /* Jay's call 2026-07-09: brand purple is the default; green survives in the
     background glow. ?theme=green flips back for comparison. */
  :root[data-theme='green'] {
    --accent: #5ed29c;
    --accent-hover: #7de3b1;
    --accent-text: #5ed29c;
    --accent-contrast: #070b0a;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    background: var(--bg);
    color-scheme: dark;
  }

  body {
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.7;
    color: var(--ink);
    -webkit-font-smoothing: antialiased;
    min-height: 100vh;
  }

  ::selection {
    background: var(--accent);
    color: var(--bg);
  }

  a {
    color: var(--accent);
    text-decoration: none;
  }
  a:hover {
    color: var(--accent-hover);
  }

  :focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 3px;
    border-radius: 2px;
  }
</style>
